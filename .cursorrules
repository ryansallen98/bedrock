Authoritative agent rules: `.cursor/rules/*.mdc` (`00-` … `14-`); docs map: `07-docs-stack-pointers.mdc`; ACFE blocks: `12-acf-blocks.mdc`; mandatory tests: `10-testing-mandatory.mdc`; mandatory Pint + ESLint: `13-mandatory-lint.mdc`; Prettier + EditorConfig: `11-frontend-formatting.mdc`; mirrors & sync: `.claude/rules/bedrock-sage-bootstrap.md`. Acorn/Laravel: `08-`, `09-`. Skills: `.claude/skills/` → `.cursor/skills/` (includes **`linting-php-eslint`**). Hub: root `CLAUDE.md`, **`AGENTS.md`**. MCP: `.cursor/mcp.json`. Sync `./scripts/sync-agent-mirrors.sh`; drift `./scripts/check-agent-mirrors.sh`.
